const canvas = document.getElementById("Drawingplace");
const toolbar = document.getElementById("  toolbar");
const canvas = document.getElementById('Drawingplace')
const ctx = canvas.getContext("2d");
